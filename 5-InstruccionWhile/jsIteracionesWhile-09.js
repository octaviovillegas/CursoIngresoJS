// Angel Farina.

function mostrar()
{
	var num;
	var nummax;
	var nummin;
	
	nummax=Math.max();
	nummin=Math.min();

	while(confirm('Quiere ingresar mas numeros?')){
		num=parseInt(prompt(''));

		if (num=nummax){

		}
	}

	txtIdMaximo.value=nummax;
	txtIdMinimmo.value=numeroMinimo;
}