function mostrar()
{

	/*var contador=0;
	//declarar contadores y variables 
	
	var respuesta="si";

	while(respuesta!="no")
	{
		
	
	}*/

//CONTADORES-----------
	/*var contadorPositivo=0;
	var contadorNegativo=0;
	var contadorCeros=0;
	var cantidadPares=0;

	//PROMEDIOS-------------
	var promedioPositivos=0;
	var promedioNegativos=0;
	//OTRAS WEAS---------
	var positivos=0;
	var negativos=0;
	var numeros;
	var respuesta="si";

	while(respuesta!="no")
	{
		numeros =parseInt(prompt("dame los numeros que quieras, los negativos los SUMO, los positivos los SUMO, CANTIDAD de positivos y negativos, CANTIDAD de ceros, CANTIDAD de numeros pares, PROMEDIO de positivos, PROMEDIO de negativos y la diferencia entre negativos y positivos"));
		if(numeros >0)
		{
			positivos += numeros
			contadorPositivo ++;
			promedioPositivos = positivos / contadorPositivo;
		}

		
		if(numeros < 0)
		{
			negativos += numeros;
			contadorNegativo ++;
			promedioNegativos = negativos / contadorNegativo;
		}


		


		respuesta  = prompt(" **ingrese NO para dejar de ingresar datos** ");
	}
document.write(			"   **** NEGATIVOS **** " + "</br>" +
						"la suma de los negativos es: " + negativos + "</br>" +
						" la cantidad de negativos es: " + contadorNegativo +"</br>" +
						" el promedio de los numeros negativos: " + promedioNegativos + "</br>" +


						"   **** POSITIVOS **** " + "</br>" +
						" la suma de los positivos es: " + positivos +"</br>" +
						" la cantidad de positivos es:" + contadorPositivo + "</br>" +
						" el promedio de numeros positivos: " + promedioPositivos + "</br>" +
						" **** CANTIDAD DE CEROS, NUMEROS PARES Y DIFERENCIA DE NEGATIVOS Y POSITIVOS ****" + "</br>" +
						" la cantidad de ceros es: " + contadorCeros + "</br>" +
						" la cantidad de numeros pares es: " cantidadPares


						);*/







}//FIN DE LA FUNCIÓN