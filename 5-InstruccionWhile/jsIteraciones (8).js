function mostrar()
{

	/*var contador=0;
	var positivo=0;
	var negativo=1;	
	var respuesta='si';
		
		alert("ingrese numeros, los positivo SUMO y los negativos MULTIPLICO");
	while(respuesta == 'si' || ( respuesta == "SI"))
	{	
		numeros = parseInt(prompt("ingrese un numero"));

	if( numeros >= 0)
	{
		positivo += numeros;
	}
	else
	{

		negativo = negativo * numeros;
	}

		respuesta  = prompt(" **ingrese SI para seguir ingresando datos** ");
	
	}

document.getElementById('suma').value=positivo;
document.getElementById('producto').value=negativo;*/




}//FIN DE LA FUNCIÓN