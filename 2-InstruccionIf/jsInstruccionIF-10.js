function mostrar() {
	let numero;
	numero = Math.floor(Math.random() * (11 - 1) + 1)
	if (numero >= 9) {
		alert(numero + " Sos un capo mostro")
	} else {
		if (numero>=4){
		alert(numero + " Aprobo")
	}
		else
		alert(numero + " Mejor la proxima pichon")
}
}//FIN DE LA FUNCIÓN